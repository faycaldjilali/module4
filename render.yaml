services:
  - type: web
    name: module4
    env: python
    plan: free
    branch: main
    repo: https://github.com/faycaldjilali/module4
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000